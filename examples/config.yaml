app:
  name: YAML Config App
  version: 1.0.0
  features:
    - authentication
    - logging
    - caching

server:
  host: localhost
  port: 3000
  ssl:
    enabled: false

database:
  type: postgresql
  host: localhost
  port: 5432
  name: myapp
  
cache:
  redis:
    host: localhost
    port: 6379